# Dependencies required to build pyembree
name: canopy
channels:
  - conda-forge
dependencies:
  - cmake
  - cython
  # - freeglut  # Not required (or available) on mac as glut is installed
  - setuptools
  - setuptools_scm
  - tbb
  - tbb-devel
