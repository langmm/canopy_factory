name: canopy
channels:
  - conda-forge
  - fredboudon
dependencies:
  - ffmpeg
  - imageio
  - matplotlib
  - numpy < 2  # Compatibility with pandas
  # - openalea.lpy  # Current conda install fails as of 9/11/2025
  - pandas
  - pvlib
  - python
  - pyshp
  - pytest
  - scipy
  - yggdrasil
